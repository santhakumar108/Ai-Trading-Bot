# Core data & numerics
pandas>=2.0.0
numpy>=1.24.0

# Market data
yfinance>=0.2.40

# Config & environment
PyYAML>=6.0
python-dotenv>=1.0.0

# News (RSS parsing, no paid API required)
feedparser>=6.0.10

# Sentiment lexicon (lightweight, no API key required)
vaderSentiment>=3.3.2

# Machine learning baseline (interpretable models only)
scikit-learn>=1.3.0
joblib>=1.3.0

# Dashboard (optional but recommended)
streamlit>=1.32.0

# Plotting (used by backtest reports / dashboard)
matplotlib>=3.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# CLI niceties
rich>=13.7.0
tabulate>=0.9.0
